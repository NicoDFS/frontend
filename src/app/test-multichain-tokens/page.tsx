/**
 * Test page for multichain token system
 * Access at: /test-multichain-tokens
 */

import MultichainTokenTest from '@/components/debug/MultichainTokenTest';

export default function TestMultichainTokensPage() {
  return <MultichainTokenTest />;
}
